There are 3 programs which are used to implement all the 3 parts of the algorithm

* ALICE1.CPP : to generate the public and private key
* BOB.CPP : to **encrypt** the message using the public key
* ALICE2.CPP : to **decrypt** the message using the private key
